[project]
name = "event-ledger-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "pydantic>=2.0",
    "pytest-asyncio>=1.3.0",
    "aiohttp>=3.9.0",
    "anthropic>=0.18.0",
    "langgraph>=0.0.26",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "pyre-check>=0.9.25",
]
